import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

// App.vue is a typed Vue SFC — the cast is safe here
// eslint-disable-next-line @typescript-eslint/no-unsafe-argument
createApp(App).mount('#app')
